/// Enum for permission check types
enum PermissionCheck {
  /// Check if user has ANY of the specified permissions/roles
  any,

  /// Check if user has ALL of the specified permissions/roles
  all,
}
